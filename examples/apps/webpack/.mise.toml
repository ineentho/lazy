[tools]
node = "22.14.0"

[tasks.install]
run = "npm install"

[tasks.lazy]
run = "cargo run --manifest-path ../../../Cargo.toml -- http webpack --cwd . --upstream-port 4103 -- npx webpack serve --config webpack.config.js"
